# NeuroEphys AI 真实数据操作手册

> 文档状态：随当前开发版本维护
> 适用范围：离线在体细胞外多通道记录
> 验证状态：Open Ephys Legacy + 32根独立微丝已完成30分钟、多记录30分钟和一段全时长真实数据验证
> 重要约束：原始文件只读；项目保存参数、派生结果和来源索引

## 1. 启动与首页

### 1.1 启动

开发环境中运行：

```powershell
python app.py
```

打包版本双击 `NeuroEphysAI.exe`。启动后先看到首页。首页提供五类数据入口和独立的项目恢复入口：

| 入口 | 用途 | 当前验证状态 |
|---|---|---|
| 新建空白项目 | 建立项目目录，再逐项导入自己的数据 | 已用真实数据验证 |
| 导入自己的数据 | 直接打开导入向导 | 已用真实数据验证 |
| 教学模拟项目 | 生成带已知神经元和行为事件的合成数据 | 已用模拟数据验证 |
| 公开验证项目 | 导入 IBL、NWB 或已排序结果 | 接口已实现；覆盖度按数据集分别标注 |

首页的“打开已有项目”用于选择 `neuroflow_project.json`。成功加载后，软件恢复上次保存的工作流位置和已有结果。

## 2. 新建自己的项目

1. 在首页点击“新建空白项目”。
2. 输入“项目名称”。
3. 在“保存到”选择项目父目录。
4. 点击“创建并进入导入”。
5. 软件建立项目目录和空白 `neuroflow_project.json`。
6. 进入“01 数据与项目”页面后点击“导入我的电生理数据”。

项目目录用于保存派生文件。原始记录仍留在原位置，NeuroEphys AI 只保存只读链接。

## 3. 导入 Open Ephys 原始记录

### 3.1 选择入口

在导入窗口选择：

- 数据来源：“我的记录系统文件”
- 记录系统：“Open Ephys”
- 数据路径：包含 `.continuous`、`.events` 和设置文件的记录节点文件夹

授权真实数据验证使用的记录文件夹结构示例：

```text
Record Node 107/
├── 100_RhythmData_CH1.continuous
├── 100_RhythmData_CH2.continuous
├── ...
├── 100_RhythmData.events
└── settings.xml
```

### 3.2 选择通道

在“通道选择”中填写需要接入当前项目的通道：

```text
1-32
```

也可以填写：

```text
1-16,21,24-32
```

通道选择影响：

- 原始质控只读取所选通道；
- sorting 缓存只写入所选通道；
- sorter 的 `n_chan_bin` 使用所选通道数；
- 项目重开后继续使用同一选择；
- 原始文件不会删除未选通道。

输入错误的通道编号时，导入应停止并显示可用范围。

### 3.3 填写记录元数据

在“记录元数据与行为（可选）”填写：

| 字段 | 含义 | 真实数据示例 |
|---|---|---|
| 电极/探针类型 | 记录接触点的物理类型 | 32通道自制微丝阵列 |
| 脑区 | 电极所在脑区 | OFC |
| contact 几何 | 下游算法是否可使用空间邻接 | 独立 contact |
| 参考/地线 | 采集时参考方式及是否保存 | 外部参考和地线，未保存为数据通道 |
| 已知坏道 | 实验人员事先确认的坏道 | 留空 |

“独立 contact”用于提醒 Kilosort 关闭虚假的空间漂移建模。自制微丝没有可靠几何坐标时，不应构造线性探针位置。

### 3.4 数据结构预览

点击“预览识别结果”。至少核对：

1. 采样率；
2. 可用通道数；
3. 所选通道；
4. 总记录时长；
5. 数据类型；
6. 电压换算比例；
7. digital event 数量；
8. 采集阶段已经使用的滤波与参考。

当前真实数据被识别为：

- Open Ephys Legacy；
- 30,000 Hz；
- int16；
- 采集时在线 250–8000 Hz 滤波；
- 采集时已使用参考处理。

在线250 Hz高通意味着低频内容已经丢失。软件会阻止 LFP、低频功率、相位和 spike-field coupling，避免把 AP 波段解释为 LFP。

### 3.5 创建项目

点击“创建并打开”。成功后可见：

- 顶部显示项目名称和项目路径；
- 数据来源卡显示 Open Ephys；
- 通道卡显示所选通道数；
- 时长卡显示接入片段时长；
- “01 数据与项目”状态变为已完成；
- 右侧运行记录新增导入信息。

## 4. 导入行为与同步信息

### 4.1 MED-PC C/D 文件

行为格式选择“MED-PC C/D”，然后选择行为文件。当前适配规则：

- C 数组：事件码；
- D 数组：事件时间，单位为秒；
- 事件码和时间按位置一一对应；
- 同步事件码：11；
- 同步关闭码：12。

已确认事件表：

| 事件码 | 事件 |
|---:|---|
| 1 | well head |
| 2 | well release |
| 3 | pump on |
| 4 | pump off |
| 5/6 | left light on/off |
| 7/8 | right light on/off |
| 9/10 | noise on/off |
| 11/12 | synchronization on/off |
| 17/18 | left lever start/end |
| 19/20 | right lever start/end |
| 21/23 | left lever on/off |
| 22/24 | right lever on/off |

### 4.2 TTL 通道

“记录中的 TTL 通道”填写 Open Ephys digital input 编号。授权项目A使用 digital input 0。其他记录必须按采集配置填写，软件不会根据文件名猜测。

### 4.3 同步方法

导入器执行：

1. 从 MED-PC C/D 读取事件码和行为时钟；
2. 选择行为事件码11作为同步锚点；
3. 从 Open Ephys `.events` 读取指定 digital input 的上升沿；
4. 按顺序匹配两组锚点；
5. 用匹配锚点估计行为时钟到电生理时钟的映射；
6. 计算残差、漂移、漏脉冲和多余脉冲；
7. 把所有行为事件转换到电生理秒时间；
8. 保留原行为时间、转换后时间和事件码。

完成后进入“06 事件同步”查看锚点数、匹配数、残差和事件清单。同步事件默认从任务事件图中隐藏，避免大量同步点遮住拉杆、灯光、泵和奖励事件。

MED-PC C/D 在当前项目中表示事件流。NeuroEphys AI 将逐行事件导出为 `tables\events.csv`，并把正式 trial 表保持为空。只有用户定义 trial 起点、终点、结果和排除规则后，软件才会生成 `trials.csv`。

## 5. 原始质控

### 5.1 目的

原始质控用于检查导入结构和记录质量。通道数、dtype、交错方式或采样率配置错误时，多通道波形、RMS和频谱通常会同时异常。

### 5.2 界面操作

1. 左侧选择“02 原始质控”。
2. 在图表面板选择需要查看的子图。
3. 使用波形工具栏缩放、平移和复位。
4. 点击底部固定按钮“运行当前所选分析”。
5. 在确认弹窗核对项目和运行节点。

### 5.3 当前计算

- 原始多通道波形；
- 通道 RMS；
- 高噪声候选通道；
- 饱和/裁剪；
- 50 Hz或60 Hz线噪声比；
- 时间分段质量趋势；
- 采集配置审计。

### 5.4 参数

真实验证的默认预览长度为8秒，时间趋势使用12个窗口。质控只给出候选异常和证据。已知坏道与算法候选坏道分别保存。

### 5.5 输出

- 结果写入 `neuroflow_project.json`；
- 结构化运行日志写入 `logs/structured_runs.jsonl`；
- 导出阶段生成 `figures/raw_qc.png` 和 `figures/raw_qc.svg`。

## 6. 预处理审计

### 6.1 目的

本页先核对采集系统已经完成的滤波和参考，再决定后续处理。重复高通、重复参考和虚构 LFP 都会改变信号含义。

### 6.2 真实数据设置

采集时已完成250–8000 Hz滤波。NeuroEphys AI：

- 保留该事实；
- 不重新执行300–6000 Hz高通；
- 不重复 common reference；
- 将 LFP 分支标记为 blocked；
- 允许查看 AP 波形预览；
- Kilosort 设置 `do_CAR=False`。

### 6.3 质量检查

处理前后图必须注明实际处理步骤。若本页只执行审计，图中不得显示虚构的滤波后数据。

## 7. Spike sorting

### 7.1 选择 sorter

1. 左侧选择“04 Spike sorting”。
2. 在 sorter 表中单击一个工具。
3. 查看“环境、结果、硬件、适用记录”。
4. 核对下方参数。
5. 点击底部“运行当前所选分析”。

软件仅执行当前选中的 sorter。失败时保留失败状态和原生日志，不会替换为其他 sorter 的已有结果。

### 7.2 Kilosort4 真实验证参数

| 参数 | 值 | 选择理由 |
|---|---:|---|
| `fs` | 30000 Hz | 来自记录头 |
| `n_chan_bin` | 32 | 当前项目所选通道 |
| `batch_size` | 60000 samples | Kilosort4 当前配置 |
| `nblocks` | 0 | 独立微丝缺少可靠连续空间几何 |
| `Th_universal` | 9 | 当前验证预设 |
| `Th_learned` | 8 | 当前验证预设 |
| `artifact_threshold` | 12000 | 当前验证预设 |
| `do_CAR` | False | 采集时已有参考处理 |
| `invert_sign` | False | 按当前记录极性 |
| `save_extra_vars` | True | 保存诊断与复核文件 |

### 7.3 输入缓存

Open Ephys 每通道文件会按1秒分块读取，写为 sorter 所需的 time-major 交错 int16 缓存：

```text
cache/sorting_input_selected_channels.bin
```

缓存只含所选通道和所选时间片段。写入后检查文件字节数。原始文件保持只读。

### 7.4 输出

Kilosort 原生文件保存在：

```text
results/kilosort4/
```

关键文件：

- `spike_times.npy`
- `spike_clusters.npy`
- `templates.npy`
- `amplitudes.npy`
- `channel_positions.npy`
- `similar_templates.npy`
- `cluster_ContamPct.tsv`
- `cluster_KSLabel.tsv`
- `kilosort4.log`
- `neuroflow_sorting_summary.json`

NeuroEphys AI 把 spike sample index 除以采样率，转换为统一的秒时间接口，同时保留全部 Kilosort 文件。

### 7.5 结果含义

Kilosort 的输出是候选 unit。进入下一页检查波形、不应期、稳定性、SNR、污染和人工标记后，才能决定是否纳入分析。

## 8. Unit 质控

### 8.1 当前指标

- spike 数；
- 平均放电率；
- 1.5 ms ISI violation；
- 正向0–50 ms ACG；
- 平均波形；
- 峰值通道；
- peak-to-peak；
- SNR；
- 振幅随时间；
- 每秒放电稳定性；
- Kilosort refractory/contamination 文件。

### 8.2 长记录性能策略

- ISI 和 ACG 使用全部 spike；
- 图中 ISI 最多均匀抽取20,000个值；
- 波形和振幅最多均匀抽取300个 spike；
- 抽样覆盖完整记录；
- ACG 使用线性内存的正向时间差累计；
- 元数据记录总数、绘图抽样数和抽样方法。

### 8.3 人工复核

自动标签只能作为候选。研究人员应同时查看波形、ACG、稳定性、振幅和 Kilosort 诊断。人工决定需要写入项目，后续统计使用明确的纳入集合。

点击“人工复核 Units”打开独立复核页。左侧列出全部候选 cluster；中间显示当前 cluster 的平均波形、ACG/不应期、ISI 分布、放电稳定性和振幅稳定性；右侧填写：

- 决策：候选单单元、多单元活动、噪声或不确定；
- 置信度：低、中、高；
- 复核者；
- 波形、ACG/不应期、振幅、记录稳定性、通道轮廓和重复/拆分风险的证据清单；
- 文字备注。

点击“保存决定”后，项目把决定和证据写入当前 sorter 对应的独立记录。切换到其他 sorter 时不会复用 Kilosort 的人工标签。

## 9. 事件分析

### 9.1 条件选择

真实验证使用事件码17与19：

- 17：左杆开始；
- 19：右杆开始。

事件码21与22在授权项目A中各67次，时间戳逐一重合。NeuroEphys AI 会报告条件时间重合并阻止将该组标签直接用于解码。

### 9.2 Raster 和 PSTH

参数：

- 时间窗：事件前0.5秒到事件后1.0秒；
- bin：25 ms；
- baseline：-0.5到0秒；
- response：0到0.5秒。

Raster 的纵轴是“事件序号”。PSTH 分别筛选两个条件，图例显示事件名称和 `n`。误差带表示事件间均值标准误。

### 9.3 群体热图

每个 unit 先用其事件前 baseline 均值和标准差进行 z-score。热图用于展示时间响应，不替代统计检验。

## 10. 统计检验

当前套件包括：

- 配对事件前后比较；
- 非参数检验；
- sign-flip permutation；
- bootstrap 置信区间；
- 效应量；
- Benjamini–Hochberg FDR。

样本单位和层级必须由研究人员确认。单只动物、单个 session 的 unit 不能直接代表动物层面的独立重复。

## 11. 机器学习

真实验证使用 Logistic regression：

- 任务：左杆开始与右杆开始分类；
- 特征：事件窗口内的 unit 放电；
- 交叉验证：5折 StratifiedKFold；
- 预处理在每个训练折内拟合；
- 指标：balanced accuracy；
- 标签置换：50次。

当前结果只验证数据进入、划分、训练、评估和导出链路。科学分析需要增加动物/session分组、预注册特征与更充分的置换次数。

## 12. Elephant/Neo 分析

已接入：

- firing-rate 统计；
- ISI；
- CV2；
- Fano factor；
- 相关关系；
- STTC。

长记录 STTC 使用线性内存实现，并用 Elephant 参考函数做数值回归。旧实现曾申请约37.4 GiB内存，当前实现不构造巨大布尔矩阵。

## 13. 图表查看与编辑

每个图表支持：

- 工具栏缩放、平移、复位和保存；
- 单击图元查看数值；
- 图表面板下拉选择子图；
- “单独放大”；
- “编辑子图”；
- “保存子图”；
- PNG、SVG导出；
- 图形工作室调整尺寸、字体、线宽、标记、颜色、坐标范围、轴宽、刻度、网格线和图例。

SVG适合继续排版。所有图需结合数据表、参数和统计表复核后再用于论文。

## 14. 保存、关闭与继续

### 14.1 保存

点击顶部“保存项目”。弹窗显示项目清单路径：

```text
<项目目录>/neuroflow_project.json
```

项目清单保存：

- 原始数据链接；
- 通道选择；
- 电极和脑区；
- 行为导入配置；
- TTL通道和同步码；
- 工作流状态；
- sorting 来源与结果索引；
- Unit QC；
- 事件分析；
- 统计和机器学习；
- 图形设置；
-结构化审计；
- 上次打开节点。

### 14.2 关闭

有未保存修改时，关闭按钮弹出：

- 保存；
- 放弃修改；
- 取消。

分析或 AI 请求仍在运行时，软件阻止退出。

### 14.3 重新打开

1. 启动 NeuroEphys AI；
2. 点击“打开已有项目”；
3. 选择 `neuroflow_project.json`；
4. 核对原始路径可读；
5. 软件回到上次保存节点；
6. 从待运行或失败节点继续。

若 Kilosort 已成功、下游失败，选择恢复现有 Kilosort 输出，避免重复 sorting。

## 15. 运行反馈和审计

运行前弹窗显示项目、节点、sorter或模型。运行中固定底栏显示：

- 当前节点；
- 当前工具；
- 数据片段；
- 已运行时间；
- 最新进度；
- 当前工作流位置。

完成弹窗显示输入、工具、结果、耗时、输出位置、警告数和下一步。失败弹窗显示失败位置、错误原因、已保留输出和恢复方式。

结构化日志位置：

```text
<项目目录>/logs/structured_runs.jsonl
```

每个节点至少有一条 `running` 和一条最终状态记录。

## 16. 受控 AI 助手

### 16.1 三种模式

- **手动模式**：关闭模型请求，全部确定性分析照常运行；
- **助手模式**：AI 解释页面、参数、错误和结果，只提供建议；
- **协作模式**：AI 可以提出白名单工具调用，用户确认后由本地模块执行。

### 16.2 配置

1. 点击顶部“AI assistant”。
2. 打开“AI settings”。
3. 选择 Provider；首个配置为 DeepSeek，也可选择 OpenAI-compatible、实验室服务或 Ollama 兼容服务。
4. 填写服务地址、模型和 API 密钥。
5. API 密钥可仅保留在会话中，或写入操作系统凭据区。
6. 点击“Preview cloud data”检查本次发送的结构化字段。

项目文件、日志、Git 仓库和报告不会保存 API 密钥。

### 16.3 AI 可以读取的内容

AI 只接收本地生成的结构化摘要，例如采样率、通道数、时长、电极、脑区、采集滤波、QC、sorter、候选 cluster、TTL、事件数、统计结果、已完成步骤、当前页面和当前选中 unit。原始电压数组和本机文件路径不进入在线请求。

### 16.4 候选工作流和工具调用

AI 返回的工作流必须使用 NeuroEphys AI 已注册的节点。用户可以取消节点、修改参数或拒绝整个计划。sorting、覆盖结果、批量运行、在线发送和长任务均要求确认。原始数据删除、覆盖和未授权传输没有对应工具。

### 16.5 科学解释格式

AI 的结果解释固定分为：

1. 已观察到的结果；
2. 统计证据；
3. 可以考虑的生物学解释；
4. 当前结果不能支持的结论；
5. 数据和方法限制；
6. 建议增加的验证。

授权项目A的 AI 回归测试确认：摘要显示250 Hz在线高通并阻断 LFP；显示30分钟 Kilosort 的4个候选 cluster；把数据提供者口头提到的8个细胞保留为未验证外部信息；AI不能直接判断哪一个数量正确。

## 17. 当前能力状态

### 已用真实数据验证

- Open Ephys Legacy直接读取；
- 选定32通道；
- 采集设置审计；
- 原始质控；
- Kilosort4；
- Kilosort恢复；
- 授权项目A全时长Kilosort结果恢复；
- 多动物30分钟Kilosort；
- MountainSort5、SpyKING CIRCUS 2和Tridesclous 2的30分钟真实数据运行；
- Unit QC；
- 人工 Unit 复核结构和保存；
- MED-PC C/D解析；
- TTL同步；
- Raster/PSTH；
- 统计；
- Logistic regression；
- Elephant spike-train 分析；
- PNG/SVG/CSV/Methods导出；
- 项目保存和恢复。
- 受控 AI 项目摘要、候选工作流、工具确认和项目级记忆恢复。

### 仅用模拟数据验证

- Neuropixels模拟；
- tetrode模拟；
-线性探针模拟；
- 多种行为任务模拟；
-部分回归、聚类和高级图形。

### 已有接口，仍需更多真实数据验证

- Intan；
- SpikeGLX；
- NWB原始 ElectricalSeries；
- IBL ALF；
-通用二进制；
- LFP和 spike-field coupling；
-远程计算。

## 18. 科学限制

1. 当前内部数据采集时已250 Hz高通，无法恢复真实 LFP。
2. Kilosort候选 unit 需要人工复核。
3. 一次 session 的解码结果不代表跨动物结论。
4. 50次置换适合技术验证，正式研究通常需要更多置换。
5. 行为事件语义依赖事件字典和实验记录。
6. Subject108 文件名与 MED-PC 内部 subject 字段存在冲突，确认身份前保留两个字段。
7. 行为箱故障动物只运行电生理、sorting和Unit QC。
