# NeuroEphys AI v1.3 验收记录

## v1.3.5 真实项目与界面修正（2026-09-22）

源码全套 **193 项测试通过**；中英文 Sphinx 手册构建成功。另用用户确认的 Subject 102 对照实际 Open Ephys/MED-PC 数据，从新项目导入、744 组 TTL 同步、完整 125 分钟 Mountainsort5 分选，到事件响应、统计、有限范围连接性、群体动态、解码、8 类事件子报告和第 11 步英文图文报告，均在保存项目中完成。项目位于工作区 `04_User_Projects/Subject_102_Reanalysis_20260922`，含选定 32 通道完整电压缓存约 14.4 GB。候选 77 簇仍须人工复核；NEX5 外部分选文件没有足够证据确定与当前原始 session 的精确时钟对应，**不是 ground truth**。300 Hz 采集高通使低频 LFP 不可分析。完整科学解释、各事件统计和项目可移植性检查均在项目的 `exports` 和 `README.txt` 中。

英文出版图文报告的第 11 步入口和八类独立子报告已核对；先前被错误继承的非当前事件群体/解码图已清除并重生成。群体图使用当前事件、20 ms 分箱，连接性一键结果仅为标明范围的筛查而非全部配对。UI 截图检查了单行工具栏、扩大后的聊天阅读区及窄窗状态。机构模型读图在代码/模拟协议层通过；本机的真实 Harness `dsh` 仍因 NVM4306 可信启动器拦截，不能宣称已取得真实视觉回答。发行安装包须另过启动、AI、图形、内置排序及 Full Kilosort 自检后方可上线。

## v1.3.3 项目对话与排序耗时审计

项目对话、旧历史迁移、搜索/分组、回车发送、Harness 图像协议及错误报告的针对性源码测试：2026-09-22，43 项通过；新增的无项目通用对话持久化和排序负载提示也有回归测试，源码全套 185 项测试进程返回 0。Harness 图像协议通过模拟服务验证；当前机器上的真实 dsh 启动受 NVM4306 限制，因此**不能声称真实模型已完成看图调用**。新发布包的封装自检和哈希以对应构建目录里的记录为准。

排序实测（2026-09-21，项目 NeuroEphys_AI_project_20260921_191450）：32 通道、30 kHz、7497.5 秒（约 125 分钟），输入缓存约 14.4 GB。MountainSort5 首次 719.367 秒，包含约 136 秒读取/重排 Open Ephys 原文件以及对 32 个无共同几何信息的独立触点逐个运行；这是保护电极拓扑正确性的串行分组，不宜未经测试改成跨触点联合聚类。Kilosort4 复用缓存后 753.278 秒，GPU 为 RTX 3080，内部报告 726.7 秒，其中 universal detection 435.3 秒、learned detection 109.0 秒、final clustering 119.7 秒。两者均完成，非卡死。真实数据没有 ground truth，两个 sorter 得到的 Unit 数差异不能作为准确率，需独立 Unit 质控和波形复核。

构建环境和 GPU 二进制的来源、校验及 Harness 限制详见 `docs/BUILD_PROVENANCE_1.3.3_ZH.md`。

## v1.3.2 AI 清晰表达修正

验收范围：

- 所有 Provider 共享自适应回答契约，不再用固定 450 汉字上限牺牲解释清晰度；
- 内部字段先翻译为科研含义，数字说明对象、单位和意义；
- 回答按需组织为结论、依据、可执行操作和重要限制，不倾倒 JSON、数组或日志；
- 简洁阅读层正确识别新的中英文标题、Markdown 强调和“你现在可以怎么做”；
- 安全边界、工具白名单、用户确认、项目隔离和完整对话审计保持不变。

本机源码验收：2026-09-18，`174 passed`；中英文 Sphinx 文档均以
warning-as-error 模式构建通过。pytest 结束后的 Windows 临时目录权限提示发生在进程
已经返回 0 之后，不影响测试结论。

Session 7 tetrode benchmark 端到端复验：132 个 trial（`lever_press` 66、
`reward_delivery` 66）、25 个 Unit、5 折 Logistic regression，balanced accuracy
0.9848、ROC AUC 0.9998；200 次标签置换 `p=0.004975`。该结果仅用于证明
工作流能恢复模拟数据中预先注入的结构，不作为生物学推断。“完整神经活动
分析包”实际导出 21 组图（PNG+SVG 共 42 个文件）、11 张表，覆盖事件
对齐、spike-train、群体动力学、连接筛查、LFP、spike-field 和呼吸案例。

v1.3.2 发行包验收：Standard 与 Full 均通过封装启动、AI 桥、图形导出、
MountainSort5 和内置 sorter 自检；Full 额外通过 Kilosort4 及 CUDA/PyTorch
组件自检。每个最终发布文件的字节数与 SHA256 以同目录、同一次构建生成的
`SHA256SUMS.txt` 为唯一依据。验收文档不内嵌其所在安装包的哈希，避免重新打包
导致循环变化或留下上一轮构建的校验值。

## v1.3.1 AI 低负担阅读层

验收范围：

- 长回答默认压缩为结论、关键点、项目证据状态、下一步和警告；
- 完整回答、科学限制、候选操作和证据编号仍可打开，且原始项目记录不被覆盖；
- 右侧助手与展开对话窗口共用“简洁／完整”偏好；
- 中英文提示词限制默认长度与项目内部过程复述，但详细请求仍可得到完整说明；
- 旧项目中既有的长对话无需迁移即可使用新阅读模式；
- 独立纯函数、AI 协议和 Qt 界面回归覆盖该行为。

本机源码验收：2026-09-17，`169 passed`；中英文 Sphinx 文档均以 warning-as-error
模式构建通过。pytest 结束后的 Windows 临时目录权限提示发生在进程已返回 0 之后，
不影响测试结果。

Full 独立打包目录和安装到全新 E 盘验收目录后均通过：文件版本/产品版本
1.3.1，启动、AI 安全桥、SVG/PDF/PNG 导出、MountainSort5、内置 sorter 集合和 Kilosort4
六项自检全部返回 0。

可选安装分支也分别在全新目录验收：

- “仅通用核心”不安装 Torch/Kilosort 目录，启动、AI、图导出、MountainSort5 和四个
  SpikeInterface CPU 内置 sorter 自检全部返回 0；
- “完整 GPU/Kilosort”安装的 Torch 二进制和 Kilosort 目录存在，上述六项自检全部返回 0；
- 安装器使用已验证 Standard 程序作为核心，再叠加 Full-minus-Standard 逐文件哈希差异，
  避免取消 GPU 后留下不完整的 Python 模块。

`NeuroEphysAI-Setup-1.3.1-Full.exe` 为 2,136,255,841 字节，SHA256 为
`834a8a06186282fa165a3edeada219675996431d601117c63ef1068d97eb7034`。发布的是最终双分支
验收包，前面验收过程中的候选包不会上传。

## v1.3.0 多 Session／多动物 Study

验收范围：

- 合成的四 Session、两动物项目：可建立/保存/重新打开 Study；
- 不同 Session 使用不同 Unit 数量，确认 trial 特征不包含跨 Session Unit ID；
- 线性 SVM 按动物整组留出、置换、置信区间、混淆矩阵和英文图导出；
- 一只动物、三个 Session 时自动按 Session 留出，并明确不做动物层级推断；
- Logistic、线性/RBF SVM、收缩 LDA 和随机森林均使用注册参数；
- 条件 A/B 必须不同且为所有纳入 Session 共有；
- Python API、CLI、App 窄窗口、后台计算、AI 工具 schema 和脱敏 Study 摘要回归；
- 完整源码测试、文档构建和发布包自检结果在发布提交与 GitHub Actions 中保留。

本机源码验收：2026-09-17，`165 passed`；中英文 Sphinx 文档以 warning-as-error 模式
构建通过。pytest 结束时 Windows 临时目录清理出现既有的权限提示，但测试进程返回 0，
不影响 165 项结果。

Full 安装包验收：安装包在 D 盘空间不足后改到明确的 E 盘发布归档目录构建，未移动项目
源码或实验数据。`NeuroEphysAI-Setup-1.3.0-Full.exe` 为 2,086,977,353 字节，SHA256 为
`f2f49be7966e7a67f67b8ad622c3c06ed0088e73e1807ea41783a871a3632f8e`。安装到独立 E 盘
验收目录后，文件版本与产品版本均为 1.3.0；打包后启动、AI 安全桥、SVG/PDF/PNG 图导出、
MountainSort5、内置 sorter 集合和 Kilosort4 六项自检全部返回 0。

科学边界：这些自动测试验证软件实现和防泄漏约束，不代表任意实验设计都满足统计功效，
也不把模拟解码分数当成真实生物学结论。跨 Session 细胞匹配尚未提供；默认明确不匹配。
