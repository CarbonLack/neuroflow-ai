# 授权真实数据验证摘要

本文记录 NeuroEphys AI 在一批已获授权、未随仓库发布的真实在体细胞外多通道电生理数据上的软件验证结果。原始数据、身份信息、实验室路径和逐动物记录均留在本地验证目录。

## 数据范围

- 记录系统：Open Ephys Legacy。
- 电极：32 根独立排列的自制微丝。
- 信号：采集时已在线进行 250–8000 Hz 滤波和参考处理。
- 行为：MED-PC 事件和 Open Ephys 数字输入同步脉冲。
- 快速回归长度：每个记录至少 1800 秒。
- 长记录回归长度：7497.489 秒。

在线高通已经移除真实低频成分。软件在这些项目中阻止 LFP、低频功率谱、LFP coherence 和 spike-field coupling。

## Sorting 验证

同一段 30 分钟记录分别运行：

| Sorter | 候选 Unit | Spike 数 | 说明 |
|---|---:|---:|---|
| Kilosort4 默认配置 | 4 | 395,007 | GPU 路线 |
| Kilosort4 灵敏度配置 | 5 | 1,176,945 | 阈值变化仅用于敏感性检查 |
| MountainSort5 | 89 | 325,982 | CPU 路线 |
| SpyKING CIRCUS 2 | 1 | 49,442 | SpikeInterface 路线 |
| Tridesclous 2 | 18 | 196,250 | SpikeInterface 路线 |

候选数量差异体现了检测阈值、聚类策略和算法假设的影响。算法输出均转换成统一的秒级 spike 接口，同时保留原生文件。准确的单神经元数量仍需波形、不应期、稳定性、重复或拆分风险以及人工复核共同判断。

长记录 Kilosort4 恢复结果包含 12 个候选 Unit 和 2,195,626 个 spike。该结果用于检查长记录恢复、下游分析和项目保存，没有被当作已经人工认证的 12 个单神经元。

## 行为与同步

- 行为事件：4,654 个。
- 同步锚点：744/744 匹配。
- 项目内部保留独立 event stream。
- 当前实验尚未配置通用 trial 定义，因此导出 `events.csv`，并删除旧版遗留的 `trials.csv`。
- 一组事件码时间戳逐一重合，软件会阻止将其直接作为两个可区分条件。
- 另一组动作开始事件用于技术链路验证，长记录纳入 168 个事件。

## 统计与机器学习边界

长记录技术验证的 Logistic regression 平衡准确率为 0.8257，标签置换检验为 `p=0.0196078`。这些数值来自软件链路回归，置换次数和事件定义尚不足以支持正式神经编码结论。

早期 30 分钟技术验证把事件码21和22作为左右条件，得到平衡准确率0.425、
`p=0.961`。后续条件审计发现两类各67条时间戳逐一重合，这组结果只保留为Bug记录。
修正后使用动作开始事件码17和19，各34条、共68条，得到平衡准确率0.7660、
ROC AUC 0.803、50次置换`p=0.0196078`。该结果仍然只用于单session软件链路验证。

## 项目恢复与日志

验证项目成功保存并重新打开，恢复了：

- 原始数据只读链接和通道选择；
- 采集设置与 LFP 阻断状态；
- QC、sorting、Unit 指标和人工复核结构；
- 行为事件与同步映射；
- 统计、机器学习和图表设置；
- 结构化运行日志、产物清单和 AI 项目历史。

完整路径、逐步操作、异常和输出索引保存在本地项目的 `exports/documentation` 目录，不进入公开仓库。
