RESEARCH DATA CATALOG
=====================

This folder documents large real-data collections that are not duplicated into
the portable example package. It contains inventories only, never substitutes
an index for actual example data.

The Wang Shufei raw dataset remains an external protected research input. Copy
that collection separately only when real-data reanalysis is required.
