# NeuroEphys AI 1.3 release notes

## 1.3.3：项目内对话、图像解读与交付一致性

- 项目对话按项目分析、图表、方法、通用与早期对话分组；自动取首问命名，也可重命名和搜索问题/回答。旧版平铺历史原样迁移到“早期对话”。
- 右侧助手与独立窗口明确区分用户和 AI 消息；回车发送、Shift+回车换行。显示层转义用户文字，避免内容被误当成界面标记。
- 当前图可在预览并确认后作为 PNG 图像块提交给已配置的 Harness SDK；不会未经同意发送原始文件。可询问软件操作、科研基础、方法选择以及通用问题，但项目数据仍以实际查询证据为准。
- 本版首次把上述源码改动纳入新的安装/便携交付，避免源码与旧版 v1.3.2 安装包混淆。图像能力的端到端验收以实际 Harness 服务测试为准。
- 排序运行时间应结合记录时长、通道/独立触点数量、首次缓存和算法阶段判断，不能单凭“数分钟”认定故障；见本版验收记录。

## 1.3.2：让回答既清楚又不冗杂

- 第 7 个 tetrode benchmark session 的 `lever_press` / `reward_delivery` 标签不再因缺少 `condition` 字段而丢失；机器学习页会先显示标签来源、各类 trial 数、有效 Unit 数和具体阻塞原因；
- 神经活动页增加“完整神经活动分析包”，一次生成全部适用图、表、数组、Methods 与 provenance，同时保留逐模块专家重跑；
- AI 上下文现在包含失败节点的具体错误和解码输入诊断，能够解释“哪里失败、为什么、如何修复”，而不是只重复通用条件；
- 修复项目重新打开后 Unit 键从整数变为字符串造成的 spike-field 导出失败；
- 修复外置构建目录下标准安装器可能误取仓库旧 `dist` 的问题；安装器现在强制绑定本次构建且已经自检通过的程序目录；
- 取消机械的 450 汉字上限，改为按问题复杂度自适应：简单问答保持短小，结果解释和操作指导允许充分说明；
- Harness SDK、OpenAI Responses 与兼容接口共享同一套回答契约；
- AI 先直接回答问题，再按需给出“为什么”“你现在可以怎么做”和“需要注意”，空小节不会强行显示；
- `qc_summary` 等内部字段必须先翻译成科研含义，数字必须说明对象、单位和意义；
- 明确区分当前项目观测、软件默认值、一般方法知识和尚未取得的证据；
- 简洁阅读层可以识别新的中英文标题和 Markdown 强调，不会把星号显示给用户，也不会漏掉实际下一步。

## 1.3.1：让 AI 真正减轻阅读负担

本版不削弱 AI 的项目读取、科学解释或受控操作能力，而是重做信息呈现层：

- 右侧助手和独立对话窗口默认采用“简洁阅读”；
- 回复先显示结论，最多三个关键点，并单列已查证据、待确认操作、下一步和重要警告；
- 完整回答、科学解释、限制、证据编号和候选操作进入“查看完整说明与依据”；
- 可随时切换“简洁阅读／完整内容”，选择会被记住；
- 历史完整回答继续写入项目对话档案，展示压缩不影响复核和审计；
- 模型默认不复述内部查询过程、接口细节、原始日志或长参数清单；明确要求详细解释时仍可展开；
- 项目总档案 ``D:\PhD\AI大赛\codex_neuroephys_ai`` 已升级为全英文、无快捷方式的
  living archive：真实保存当前源码 ZIP、完整 Git bundle、发布与验证文档，并以英文清单
  记录大型数据和安装包的权威位置。Git 提交/切换/合并和发布打包后自动刷新；
- Full 安装器的“仅通用核心／完整 GPU+Kilosort”均使用独立验收过的程序负载，
  取消 GPU 时不会留下不完整的 Torch 模块。

## 1.3.0：多 Session／多动物 Study 层

本版在既有单 Session 十一阶段流程之上增加独立 Study 工作区。单个项目的导入、质控、
sorting、Unit 复核、行为同步和事件分析保持不变；完成后的项目可以被 Study 只读索引，
用于跨 Session／跨动物汇总。

### 新增能力

- 文件菜单、首页和 Ctrl+K 操作查找中增加“多 Session 研究”；
- Study 清单显式记录生物学动物编号、唯一 Session 编号、纳入状态、项目状态和共有条件；
- 用户可选择两个共有条件、Logistic、线性/RBF SVM、收缩 LDA 或随机森林；
- 按整只动物或整个 Session 留出，标准化仅在训练折拟合；
- 逐留出组性能、混淆矩阵、组内标签置换、分组 bootstrap 区间；
- Session 条件效应汇总；动物数足够时尝试带动物随机截距和 Session 方差分量的混合模型；
- 固定维度群体分布特征，不默认匹配跨 Session Unit ID；
- 描述性 PCA + 正则化线性状态转移轨迹；
- 英文 SVG/600 DPI PNG、CSV 与 JSON 完整导出；
- Windows App 后台计算、窄窗口纵向重排；
- Python API 与 `study-create`、`study-add`、`study-inspect`、`study-run` CLI；
- AI 上下文可读取脱敏 Study 摘要，并在协作模式提出受控运行；原始电压、本地路径和
  动物/Session 明细不会进入云端摘要。

### 科学边界

- 只有一只动物时自动退回 Session 留出，不宣称跨动物泛化；
- 相同 Unit 编号不是跨 Session 细胞身份；
- LDA 是监督分类器，潜在动力学是另一个描述模型；
- 解码性能只说明指定验证设计下存在可预测信息，不等于因果或神经机制；
- 极少动物时不提供随机效应确认性推断。

完整操作与方法说明见 `docs/MULTI_SESSION_ANALYSIS_ZH.md` 和双语网页手册的
“统计、机器学习与科学边界”。
