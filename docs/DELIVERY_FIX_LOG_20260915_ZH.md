# 三批数据验收：修正与未完成事项

## 本轮已实际执行

- 两套模拟数据各取 session_01，均为完整20分钟；不是仅运行截图中的100毫秒，也不是所有14个模拟session均已验收。
- 从原始二进制新建项目，再单独导入行为事件，实际运行分选。
- tetrode：Kilosort4、MountainSort5、SpyKING CIRCUS 2均完成，候选簇分别26、24、25。
- 独立真值评估的全体真实单元平均召回率分别约0.964、0.871、0.952；候选簇仍需人工复核。
- Neuropixels：Kilosort4和MountainSort5完成，但效果不足，平均召回率分别约0.212和0.367；第三种工具正在处理。不能把完成计算称为分选质量达标。

## 已修正的问题

1. **单条件图中出现不存在的other组**：计算保留兼容结构，绘图不再显示零事件组。原因是内部双组占位被直接用于图例。
2. **导出说明声称运行了实际没运行的分析**：原有模板写死了Demo名称、步骤和解码方法。现在使用项目名称、实际非空结果、真实事件窗口和分箱参数。结果存在不等于科学验收通过。
3. **真值匹配可能漏掉可用配对**：最近邻去重不总能取得最大的一对一匹配，已改成有序双指针匹配，增加反例测试。
4. **只看配对中位数可能掩盖漏检**：增加全体真实单元平均召回率、未配对单元清单、F1≥0.8恢复单元数；移除把真实sorter输出误称为“故意生成错误的候选”的固定描述。

## 真实数据边界

- 动物编号、行为文件和电极组必须独立核对，不能只凭文件排序认定，不跨动物分选。
- 文件名与文件内部身份不一致时记录冲突，不静默改名。
- 采集时已经去除的低频信号不能恢复，对应分析明确跳过。
- 算法独立接触点布局不是实测坐标，未知几何的微丝记录需检查跨接触点重复unit。
- 分段插值在训练锚点处的零残差不等于同步误差为零；保留全局拟合与匹配证据。
- 具体动物身份、同步数值及私有实验结果仅保存在本地实验报告。

## 文件位置与交付状态

## 后续定位：多探针几何

Neuropixels模拟CSV包含两根探针，各自使用相同原点的局部坐标。验收导入脚本未保留probe_id，导致算法看到重叠接触点；SpyKING CIRCUS 2明确报重复坐标。现已在App通用二进制导入中读取channel_geometry.csv，保留探针标识，并为算法提供分离的坐标系。平移量不代表解剖距离。旧输出保留，新输出写入geometry_corrected子目录，尚需比较修正后的独立评估，不能提前认定全部问题解决。

出版导出已增加英文图件（不改变App语言）、子图字母、主图/附图索引、英文图注草稿、中文阅读说明和带校验值的完整文件清单。它是可审核的排版草稿，尚不是已经完成期刊技术检查的最终稿。

实验输出：`D:\PhD\AI大赛\NeuroEphysAI_Workspace\Delivery_Validation_20260915`。

每个数据/工具子目录包含项目清单、运行日志、原生分选结果、导出图表、独立评估。根目录`分选评估汇总.md`解释指标。

尚未完成：三批全部下游分析、全部动物身份核验、逐图审阅、最终主图/附图排版、更新App安装包和最终验收。此文不是完成声明。私有实验原始数据和结果不提交公开GitHub，代码、通用说明与修正记录提交。
