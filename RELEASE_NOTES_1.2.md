# NeuroEphys AI 1.2 — 操作体验与手册

## 1.2.6：发布版本标识修正

- 保留 1.2.5 的全部 Harness SDK、MCP、对话与受控分析功能，不改动科学计算。
- Windows exe 的数字版本和显示版本改为从 PRODUCT_VERSION 自动生成，移除遗留静态版本模板，避免主界面是新版、文件属性却仍显示旧版。
- 安装器输出目录也从构建版本推导，并增加版本资源回归测试。

## 1.2.5：项目查询与连续科研对话

- 新增官方 Harness SDK 协议连接，模型调用由本机 dsh 负责；不同于旧版仅发现连接配置后使用兼容 API 的方式。
- 新增受限本机 MCP：当前页面/项目、分页数据、事件、Unit、分析结果、历史对话查询及分析提案。
- 对话自动保存到项目，查询保存实际读数和快照证据；每轮更新快照，项目之间隔离。
- 提案、拒绝和确认执行分离；执行结束后回读任务记录与结果，不将“建议”写成“已完成”。
- 小窗聊天工具分行，空计划表不占空间；保持既有紫黑配色和本地科学分析功能。
- MCP 不开放任意代码、shell、原始电压或文件系统。Harness 须独立安装配置，不在安装包中携带机构凭据。
- 可重复验收脚本与边界见 `docs/AI_HARNESS_IMPLEMENTATION_1.2.5_ZH.md`。

## 1.2.4：DeepSeek Harness 原生接入

- AI 设置可一键读取本机 DeepSeek Harness 的 Provider、地址、模型列表和密钥环境变量名，但不读取 Harness 凭据文件。
- 机构内网 HTTP 端点默认禁用，只能对单个可信 Provider 显式开启；公网 HTTP 仍被拒绝。
- 修正 Provider 凭据隔离，避免程序化创建设置时误用其他 Provider 的默认密钥。
- 兼容 Harness 的紧凑工具建议格式；建议仍必须经过 App 本地白名单、参数 Schema、风险与确认规则，不会直接执行。
- AI 设置、云端内容预览和完整助手在窄窗口中自动纵向排列；通用弹窗增加屏幕范围保护。
- 已实机验证机构模型列表、`deepseek-v4.1-flash` 最小请求与 `inspect_project` 受控建议；测试不发送原始电压和本地路径，也不执行 AI 建议的操作。

## 1.2.3：决赛交付、受控 AI 与完整科研叙事

- 决赛准备页把项目价值、AI 作用、真实证据、5–10 分钟演示顺序和离线备用方案放进 App，并只对真实存在的证据标记完成。
- AI 新增机构官方 harness（OpenAI-compatible）入口；发送内容升级为版本化项目摘要，包含当前流程、已完成分析、可用图表和受注册工具约束的操作契约，原始电压和本地路径仍不上传。
- AI 设置、上下文预览和完整 AI 工作台均支持小窗口缩放；长设置使用滚动区，不再强制占满桌面。
- 英文论文导出按实验背景与质量、事件/群体响应、效应/不确定性/预测组织主图，其余结果进入补充图；每张图均进入清单并获得 a–d 面板字母和可编辑图注草稿。
- 两套 20 分钟模拟 benchmark 已完成三 sorter、ground-truth、事件、统计、导出和 407 张 SVG 完整性检查；错误几何版本只保留为诊断，不作为有效结论。
- 王淑霏真实 session 按确认动物映射运行；12 点记录的 33–64 通道明确排除在动物/行为汇总之外。候选簇、自动筛选暂留与人工确认 Unit 始终分开报告。
- 修复 Windows GitHub runner 在传统控制台编码下输出中文文件名导致的发布失败。
- 新增 `codex_neuroephys_ai` 规范入口，以目录链接分类源代码、软件发布、原始数据、项目、验证、论文复现和交接资料，不复制大型数据或破坏旧项目绝对路径。

## 1.2.2：标准安装版与 GPU Full 离线版

- GitHub 默认下载改为较小的标准安装版和标准便携版；两者包含桌面界面、导入、质控、已有 sorting、Unit 复核、行为、统计、机器学习、绘图、AI 控制和教程。
- Kilosort4、PyTorch/CUDA 等大体积 GPU 运行组件进入明确命名的 `-Full` 离线安装包，不再让普通用户误以为必须下载数GB才能打开软件。
- Sorter 管理页新增“获取 GPU Full 离线版”入口，指向与 App 版本一致的官方 Release；缺失 Kilosort 时保持不可用，不静默替换。
- 构建脚本从文件名层面区分标准版与 Full 版，防止相互覆盖或发布时混淆。
- Full 安装程序新增“完整 GPU/Kilosort / 仅通用核心 / 自定义”安装类型；默认项仍是经过本机验收的满血配置。
- Release 同时提供 Full 自选安装版、标准安装版、标准便携版和 Python wheel，并在下载说明中明确推荐场景。
- 原始实验数据、完整20分钟 benchmark 和大体积派生结果不进入 App 安装包；需要共享时作为独立数据资产和清单发布。
- 新增 D 盘项目总导航和目录清单；历史阶段结果保留原路径，避免破坏项目清单中的绝对链接。

## 1.2.1：科研作图与格式编辑

- 将含糊的 Axis 编号改为实际子图标题，双击能正确定位，预览跟随所选子图。
- 参数按标题、范围、位置、轴线、刻度、网格等分页并排展示。
- 增加科研通用 / Nature 参考 / 报告预设，以及当前子图、整图、项目所有图三种范围。
- 项目统一样式和局部覆盖随项目保存；界面和 Python 重绘、报告使用相同默认参数。
- 修复短记录末端空波形窗；不改科学分析算法和原始数据。
- 规范及官方来源见 docs/FIGURE_STYLE_STANDARD_ZH.md；App 紫黑配色不变。

## 1.2.0

本次沿用紫黑配色与原 Logo，重点改善已有功能的可发现性、阅读和窗口空间分配。

- 教程中心改为 18 项具体任务，可搜索、分类、调字号、标记已读；操作、参数和排错分开查看。
- 教程可独立于工作区打开，并跳转到相应页面；正文使用明确的深色主题文字，取消横向长表。
- App 和网页手册共用任务内容；新增第一次练习、工作区说明和更新后的界面截图。
- 首页精简为居中的 Logo、项目入口、功能查找和最近项目。配色与 Logo 边框保留。
- 工作区采用统一线条图标、紧凑导航、较轻的标题和边框。图表工具可折叠，控件在窄栏换行。
- Ctrl+K 查找操作和最近项目；Ctrl+Shift+H 打开教程中心。原有快捷键继续可用。
- 最近项目记住最近 8 个打开入口；路径失效条目显示为不可用。
- 单步运行按本次任务数计算进度；切换项目时检查未保存修改和运行中的任务。

查看新界面：帮助 → 教程中心；首页 → 教程中心；视图 → 查找操作。
更改图形：展开“图表工具”。调整窗口：Ctrl+B、Ctrl+J、拖动三栏分隔线。

本轮没有新增 sorter 算法，已有数据、统计与分析方法保持可选择。
预处理桌面页是预览，不是通用处理链编辑器；Unit 复核保存标签、证据与备注，不提供通用合并/拆分工具。
真实记录的 sorter 一致度需要核对时间窗、训练条件与来源，不能解释为准确率。

发行文件包括 Windows 安装版/便携版和 Python wheel/源码包。核心版与包含 Kilosort/GPU 运行库的完整包分别标识。
支持环境为 Windows 10/11 x64、Python 3.12；GPU sorter 依赖兼容硬件和驱动。
改进依据与官方资料见 docs/PRODUCT_REVIEW_1.2_ZH.md。

本地正式发布脚本默认生成完整分析版，并在打包后实际运行 Kilosort4、MountainSort5、
内置 sorter、启动、AI 隐私和图形导出自检；只有显式传入 `-Lite` 才生成公开核心包。
