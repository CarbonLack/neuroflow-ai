# NeuroEphys AI 1.2 验证记录

本文件随代码、桌面包和 Python 包交付。验证日期：2026-09-08。

## 已完成

- 源码回归测试：107 项通过。30 条第三方库弃用提醒，不是运行失败。
- 中英文网页手册：严格构建通过；桌面宽度和 390 px 窄屏已查看真实浏览器显示。
- Windows 原生渲染截图：已检查首页、教程、阶段引导、1500 px 三栏和 900 px 窄窗。
- 新增保护测试：最近项目排序与失效路径、切换项目取消/保存失败、任务运行期间禁止切换、教程精确跳转和单步进度。

## 已完成的打包与文档验收

- 完整桌面程序：PyInstaller 构建成功，启动检查通过。
- 图形导出：打包程序生成 SVG、PDF、PNG，通过。
- AI：离线上下文与执行确认保护通过；未向外部模型发送实验数据。本检查不等于所有云模型服务均可用。
- Kilosort 4.1.7：实际在 RTX 3080、PyTorch 2.11.0 + CUDA 12.8 环境运行教学数据，保存原生结果，通过。
- MountainSort5：实际运行教学数据，通过。
- SpyKING CIRCUS 2、Tridesclous 2、Simple、Lupin：实际运行教学数据并生成横向比较，通过。
- Python wheel 与源码发行物：构建成功，twine 元数据检查通过。
- 离线网页资源与锚点：39 页、7,995 个本地链接，0 错误。
- GitHub Pages：提交 6ee6f91 对应构建 34190956246 已成功部署。

参数参考已与 App 操作说明分离，不再把底层库的方法参数全部描述成桌面按钮。
发布版包含的 sorter 取决于所选发行物：Full 包包含 GPU 运行库；核心包按实际环境探测。
这些检查确认列出的路径可运行，不代表所有设备、所有参数或任意规模记录都已验证。

测试数据为教学模拟或现有测试夹具；本次界面优化不声称新增真实数据验证结论。

## 1.2.1 图形工作室验证（2026-09-14）

- 源码回归测试：113 项通过。
- 真实 Windows 渲染已检查 1280 × 820 和 920 × 700 两种窗口：设置项按类别平铺，作用范围在小窗口中仍可直接看到，预览区保持完整。
- “坐标轴 1 / 2”已更名为带真实标题的“子图 1 / 2”，并修复左对齐标题未被识别的问题。
- 新增当前子图、当前整图、项目所有图三种统一样式作用范围；项目样式随项目文件保存，重新生成图时自动恢复。
- 默认分类色为低饱和浅紫、柔绿、浅赭、灰蓝；高对比分类色和原始配色仍可选择。连续热图的色标、范围和归一化保持不变。
- 已增加回归保护：样式切换可逆；标题或背景编辑不改变坐标范围、比例、定位器、绘图区位置与已有网格；短记录原始波形窗口不会产生空切片。
- SVG 保留可编辑文字，默认位图导出为 600 DPI；89 mm / 183 mm 画幅参考来自 Nature 最终投稿说明。
- Full 桌面目录、便携 ZIP 和 Inno Setup 安装包均构建成功；安装包已在本机静默升级，桌面快捷方式重新生成。
- 已从安装后的程序再次执行启动、离线 AI 隐私与 SVG/PDF/PNG 导出自检，全部通过；窗口标题确认为 v1.2.1。
- 本机成品位置：`D:\PhD\AI大赛\本地正式版\v1.2.1`；安装及成品自检记录：`D:\PhD\AI大赛\发布验证\v1.2.1`。

上述预设是投稿前的规范化起点，不代表对所有期刊自动合规。最终仍应依据目标期刊和最终版面尺寸检查。

## 1.2.1 正式 Full 发行物最终验收（2026-09-16）

- 源码完整回归：131 项通过；其余输出为第三方科学计算库的弃用提醒，不是失败。
- 正式构建默认是 Full 包，包含 Kilosort 4 所需的 PyTorch/CUDA 运行组件；只有显式使用 `-Lite` 才会生成不含这些组件的核心包。公开 GitHub Actions 仍明确构建 Lite 包，避免把缺少 GPU 组件误写成完整包。
- 构建目录中的正式程序依次通过六项阻断式自检：启动、离线 AI 隐私边界、SVG/PDF/PNG 图形导出、MountainSort5、内置 sorters、Kilosort 4。任一项失败时构建脚本都会停止，不生成“已通过”的发行结论。
- Kilosort 4.1.7 实际使用 PyTorch 2.11.0 + CUDA 12.8，在 NVIDIA GeForce RTX 3080 20 GB 上完成模拟记录分选；结果为 2 个 unit、771 个 spike，并保存原生诊断文件。
- 安装包 `NeuroEphysAI-Setup-1.2.1.exe`（2,081,209,762 字节）已静默安装到隔离验收目录，退出码为 0，无需重启；桌面和开始菜单的一键快捷方式均成功生成。安装后的程序再次通过启动和 GPU Kilosort 实跑。
- 便携包 `NeuroEphysAI-1.2.1-Windows-x64-portable.zip`（3,129,898,674 字节）已完整解压；解压后的独立程序再次通过启动和 GPU Kilosort 实跑。
- Python wheel `neuroephys_ai-1.2.1-py3-none-any.whl`（426,688 字节）已安装到独立目录；从非源码工作目录成功导入 `neuroephys` 和 `neuroflow`，产品版本为 1.2.1，公开 API 为 60 项。发行名是 `neuroephys-ai`，代码导入名是 `neuroephys`。
- 各发行文件的 SHA-256 校验值保存在同目录 `SHA256SUMS.txt`。正式成品位于 `D:\PhD\AI大赛\neuroflow-ai\release\v1.2.1`；独立验收证据位于 `D:\PhD\AI大赛\发布验证\v1.2.1_d1eaa4a_build`、`v1.2.1_d1eaa4a_install`、`v1.2.1_d1eaa4a_portable` 和 `v1.2.1_d1eaa4a_wheel`。

这里的“通过”限定为上述机器与明确列出的自检数据，不代表任意第三方数据、任意驱动或任意参数组合均自动通过。真实数据必须继续保留原始文件、参数、日志、人工 Unit 复核与失败记录。

## 1.2.2 三路线与组件式安装验收（2026-09-16）

- 源码完整回归：132 项通过；311 条输出为第三方科学库弃用或数值边界提醒，不是测试失败。
- 本版同时生成 Full 自选安装版、标准安装版、标准便携版和 Python wheel。Full 安装器的默认类型是“完整 GPU/Kilosort”，也可选择“仅通用核心”或自定义组件；不是只改变下载文件名。
- `NeuroEphysAI-Setup-1.2.2-Full.exe` 为 2,081,261,980 字节，低于 GitHub 单文件 2 GiB 限制。它分别以 `/TYPE=full` 和 `/TYPE=compact` 安装到隔离目录，退出码均为 0。
- Full 默认安装占用约 4.78 GiB，实际包含 Torch 与 Kilosort；安装后的程序通过启动、离线 AI 权限边界、SVG/PDF/PNG 导出、MountainSort5、内置 sorter 和 Kilosort 4 六项自检。Kilosort 再次在 NVIDIA GeForce RTX 3080 20 GB 上实际运行，不是仅检查模块存在。
- 同一个 Full 安装器选择“仅通用核心”后占用约 0.80 GiB；启动、AI 和图形导出通过，Kilosort 负向检查退出码为 1，证明未安装的 GPU 后端不会被误报为可运行。
- `NeuroEphysAI-Setup-1.2.2.exe` 为 244,446,912 字节；独立标准安装后的启动、AI 和图形导出通过。标准安装目录不含 Torch/Kilosort。
- `NeuroEphysAI-1.2.2-Windows-x64-portable.zip` 为 345,463,383 字节；完整解压后的程序再次通过启动、AI 和图形导出。不能只复制其中的单个 EXE。
- `NeuroEphysAI-1.2.2-Windows-x64-Full-portable.zip` 为 3,129,902,527 字节，保留作本地完整归档；因超过 GitHub 单文件限制，不列为普通用户的线上下载入口。
- `neuroephys_ai-1.2.2-py3-none-any.whl` 为 427,527 字节；安装到隔离目录后从源码仓库之外导入成功，版本为 1.2.2，公开 API 为 61 项。
- 安装与自检证据位于 `D:\PhD\AI大赛\发布验证\v1.2.2_*`。正式文件及 SHA-256 位于 `D:\PhD\AI大赛\neuroflow-ai\release\v1.2.2`。

组件式安装验证只证明本机上三条安装路径工作正确。Kilosort 仍要求兼容 NVIDIA GPU 和驱动；Full 安装包包含 GPU 运行库不等于任意电脑都能运行 GPU sorter。

## 1.2.3 决赛交付验收（2026-09-16）

- 源码完整回归：135 项通过；随后新增的离线手册识别、已确认动物筛选和小窗 AI 布局分别通过定向测试与离屏实例化检查。测试结束时仅有本机 pytest 临时目录清理权限提示，不是测试失败。
- Windows 标准安装包 `NeuroEphysAI-Setup-1.2.3.exe` 为 244,465,958 字节；标准便携包为 345,493,691 字节。
- Windows Full 自选安装包 `NeuroEphysAI-Setup-1.2.3-Full.exe` 为 2,081,280,458 字节，仍低于 GitHub 2 GiB 单文件上限；Full 便携归档为 3,129,930,926 字节，只作本地完整备份。
- Python wheel 为 432,164 字节，源码包为 447,954 字节；两者通过 twine 元数据检查。
- 构建过程中的标准版和 Full 版均实际运行阻断式启动、AI 隐私、SVG/PDF/PNG 导出、MountainSort5 和内置 sorter 自检；Full 版另运行 Kilosort4 自检。
- Full 安装包已静默安装到 `D:\PhD\AI大赛\本地正式版\v1.2.3`。安装后的独立程序再次通过启动、AI、图形导出、MountainSort5、内置 sorter 与 Kilosort4 六项自检，证据位于 `D:\PhD\AI大赛\发布验证\v1.2.3_installed_full`。
- 两套 20 分钟模拟项目的三 sorter 全链条已完成；407 张最终范围 SVG 的机械完整性审计为 0 个标记项。王淑霏真实 session 继续按已确认动物映射生成候选簇、自动筛选、行为同步、事件分析和三 sorter 比较；人工 Unit 复核仍是投稿前必需步骤。

这些验收证明列出的构建、测试项目和机器可运行，不把自动筛选候选簇等同于人工确认单元，也不保证所有第三方电脑、GPU、驱动和任意数据参数都无需适配。
