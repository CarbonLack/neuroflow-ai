stages:
  - deploy

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -R docs/site/. public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
