deploy-pages:
  stage: deploy
  image: alpine:3.21
  script:
    - test -f docs/site/index.html
    - mkdir -p public
    - cp -R docs/site/. public/
  pages: true
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
