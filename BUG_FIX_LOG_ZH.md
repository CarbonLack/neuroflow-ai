# NeuroEphys AI Bug修复日志

本文记录已发现问题、修复依据、回归范围和剩余风险。状态分为：

- **已回归**：自动测试或真实数据复测通过；
- **局部回归**：源码路径通过，仍需完整安装包或更多数据；
- **待验证**：已有修复，尚缺目标环境复测。

## BUG-001 sorter环境检查触发UTF-8解码崩溃

**问题现象：** 启动打包程序时直接退出，提示 `utf-8 codec can't decode byte 0xb2`。
**出现位置：** sorter目录刷新，第三方HDsort编译检查。
**如何复现：** Windows中文系统启动旧打包版，`SpikeInterface installed_sorters()`探测所有sorter。
**错误信息：** `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xb2`。
**影响范围：** 软件启动、Sorter管理器、环境刷新。
**根本原因：** SpikeInterface探测HDsort时启动外部命令，系统输出按UTF-8强制解码；Windows本地编码产生非UTF-8字节。
**修复方法：** NeuroEphys AI逐个、隔离地探测已支持sorter；捕获每个后端的环境异常；环境检查失败转为该sorter的诊断状态。
**为什么这样修：** 一个可选sorter的环境故障不应阻止主程序启动。
**回归测试：** `tests/test_ui_panels.py`覆盖窗口构建和Sorter工作台；Windows源码版启动通过。
**修复结果：** **已回归**，环境探测异常显示为单项状态。
**剩余风险：** 新增第三方sorter仍需为其外部命令编码增加适配。

## BUG-002 PyInstaller遗漏XGBoost VERSION文件

**问题现象：** 打包版启动时在导入XGBoost阶段崩溃。
**出现位置：** `neuroflow.decoding`模块导入。
**如何复现：** 运行旧版 `dist\NeuroFlow\NeuroFlow.exe`。
**错误信息：** `FileNotFoundError: ...\xgboost\VERSION`。
**影响范围：** 整个打包程序，机器学习页面尚未打开也会发生。
**根本原因：** 顶层直接导入XGBoost，PyInstaller未收集包数据文件。
**修复方法：** 可选模型改为延迟导入；打包配置收集XGBoost数据；XGBoost不可用时仅禁用对应模型。
**为什么这样修：** 可选机器学习后端不应成为程序启动依赖。
**回归测试：** 源码导入和机器学习测试通过。
**修复结果：** **局部回归**。
**剩余风险：** 最终发布前需重新构建Windows安装包并在无开发环境电脑上启动。

## BUG-003 Kilosort运行出现NoneType.write

**问题现象：** 点击Kilosort4后弹出 `'NoneType' object has no attribute 'write'`。
**出现位置：** Kilosort日志输出与GUI后台线程。
**如何复现：** 旧打包版在无有效标准输出流时运行Kilosort。
**错误信息：** `AttributeError: 'NoneType' object has no attribute 'write'`。
**影响范围：** Kilosort运行、日志和结果注册。
**根本原因：** GUI打包进程的 `sys.stdout`/`sys.stderr` 为空，第三方工具尝试写控制台。
**修复方法：** 后台执行提供安全日志出口；原生Kilosort日志写入结果目录；失败时保留现有文件并支持恢复。
**为什么这样修：** 科研计算需要完整日志，吞掉输出会降低可追溯性。
**回归测试：** 授权项目A、B、C的30分钟Kilosort4真实运行成功。
**修复结果：** **已回归**。
**剩余风险：** 最终PyInstaller安装包仍需再跑一个短sorting smoke test。

## BUG-004 SVG后端未打包

**问题现象：** 保存SVG时报 `No module named matplotlib.backends.backend_svg`。
**出现位置：** 单图保存和可复现导出。
**如何复现：** 旧打包版点击“保存子图”，格式选择SVG。
**错误信息：** `ModuleNotFoundError: matplotlib.backends.backend_svg`。
**影响范围：** 矢量图导出。
**根本原因：** PyInstaller未发现Matplotlib动态选择的SVG后端。
**修复方法：** 打包配置显式收集SVG后端；源码导出统一通过Figure API。
**为什么这样修：** SVG是后续论文排版的重要中间格式。
**回归测试：** UI测试实际保存子图SVG并检查文件大于500 bytes；真实项目批量导出SVG通过。
**修复结果：** **局部回归**。
**剩余风险：** 新打包产物需要独立验证。

## BUG-005 长记录STTC申请约37.4 GiB内存

**问题现象：** 30分钟真实数据运行Elephant相关分析时申请约37.4 GiB并失败。
**出现位置：** spike time tiling coefficient。
**如何复现：** 对高spike数unit使用旧广播矩阵实现STTC。
**错误信息：** NumPy内存分配失败，申请约37.4 GiB。
**影响范围：** 长记录、多unit的spike关系分析。
**根本原因：** 旧实现构造两条spike train的两两时间差矩阵，复杂度与内存为 `O(N×M)`。
**修复方法：** 使用排序spike数组和双指针计算覆盖比例与近邻命中，内存复杂度为 `O(N+M)`。
**为什么这样修：** STTC只需要窗口覆盖与是否存在近邻，无需保存全部两两差值。
**回归测试：** `test_linear_memory_sttc_matches_elephant_reference`与Elephant参考函数数值一致；授权项目A、B、C的30分钟分析完成。
**修复结果：** **已回归**。
**剩余风险：** 极端重复时间戳和边界值继续保留单元测试。

## BUG-006 Open Ephys分段读取忽略start/end frame

**问题现象：** 设置片段后仍读取记录起点或完整记录。
**出现位置：** Open Ephys Legacy自定义数组和SpikeInterface RecordingSegment。
**如何复现：** 项目设置 `start_frame=10`、`end_frame=20`，读取前两个值。
**错误信息：** 无异常，返回内容位置错误。
**影响范围：** 时间片段分析、sorting缓存、事件边界。
**根本原因：** 局部索引没有加到源记录的start frame；end frame未参与shape。
**修复方法：** 数组shape使用切片范围；每次读取把局部frame换算到源frame；end frame参与边界检查。
**为什么这样修：** 片段选择必须在数据适配器底层统一生效。
**回归测试：** `test_open_ephys_legacy_is_linked_and_read_on_demand`验证10–20 frame读取值。
**修复结果：** **已回归**。
**剩余风险：** 需要为多segment新版Open Ephys数据增加同类测试。

## BUG-007 行为图把事件称为Trial

**问题现象：** 每个MED-PC事件被标为一个Trial。
**出现位置：** 行为分析图。
**如何复现：** 导入C/D事件后进入行为页。
**错误信息：** 无异常，图语义错误。
**影响范围：** 用户理解、论文图。
**根本原因：** 通用事件列表复用了trial型示例图标签。
**修复方法：** 非IBL trial结构改为“任务事件计数 + 事件时间轴”；事件行统一称为事件；Raster改为事件序号。
**为什么这样修：** trial需要实验规则组合多个事件，单个事件不能自动等同trial。
**回归测试：** Subject102行为图人工检查；`test_real_event_figures_use_event_semantics_and_localized_conditions`检查标签。
**修复结果：** **已回归**。
**剩余风险：** 用户仍需提供正式trial起止与排除规则。

## BUG-008 行为事件标签拥挤

**问题现象：** 大量事件名称全部挤在横轴，同步事件遮住任务事件。
**出现位置：** 30分钟行为图。
**如何复现：** 导入数千个MED-PC事件。
**错误信息：** 无异常，图不可读。
**影响范围：** 行为质量检查。
**根本原因：** 把类别标签作为时间轴刻度，并把同步和任务事件画在同一层。
**修复方法：** 左图使用水平计数条；右图使用任务事件时间轴；同步事件默认隐藏；中文/英文名称随语言切换。
**为什么这样修：** 类别数量和时间分布需要两个不同的视觉编码。
**回归测试：** Subject102 30分钟图人工检查；行为图测试确认时间轴无重复ylabel。
**修复结果：** **已回归**。
**剩余风险：** 事件类型超过约20种时需要分页或筛选。

## BUG-009 左右条件PSTH可能复用同一数据

**问题现象：** 左右两条PSTH完全重合，怀疑代码复用。
**出现位置：** event-aligned analysis。
**如何复现：** 授权项目A选择事件码21和22。
**错误信息：** 无异常，结果可疑。
**影响范围：** 条件比较、统计和解码。
**根本原因：** 代码分别筛选条件；真实事件码21与22的67个时间戳逐一重合，因此输入窗口完全相同。
**修复方法：** 增加条件事件数、成对时间重合比例和容差诊断；时间戳高度重合时阻止监督解码；真实验证改用17与19。
**为什么这样修：** 曲线重合可能来自真实输入，也可能来自代码错误，软件需要提供可检查证据。
**回归测试：** 授权项目A输出显示21/22为67/67完全重合；17/19分别筛选并得到不同输入。
**修复结果：** **已回归**。
**剩余风险：** 实验设计中同时发生的不同事件仍需研究人员决定分析语义。

## BUG-010 项目保存、退出和恢复不完整

**问题现象：** 用户担心关闭后无法从上一步继续。
**出现位置：** 保存、关闭、首页打开已有项目。
**如何复现：** 修改参数或导航后关闭；重开项目。
**错误信息：** 旧版无明确提示或恢复项不完整。
**影响范围：** 长流程、科研审计、结果重复使用。
**根本原因：** 缺少dirty状态、关闭守卫和完整序列化。
**修复方法：** 保存数据链接、配置、结果、图形设置、日志和最后节点；关闭时提供保存/放弃/取消；运行中阻止退出。
**为什么这样修：** sorting和下游分析可能持续数小时，项目必须可恢复。
**回归测试：** `test_saved_project_reopens_at_the_last_stage`、`test_unsaved_close_save_choice_persists_project`；授权项目A/B/C重载通过。
**修复结果：** **已回归**。
**剩余风险：** 移动原始文件后仍需增加图形化“重新定位数据源”。

## BUG-011 sorter选择失败后显示Kilosort旧结果

**问题现象：** 选择其他sorter后失败，页面仍显示Kilosort图，用户误以为运行成功。
**出现位置：** sorter工作台和结果激活。
**如何复现：** 已有Kilosort结果，选择未安装sorter并运行。
**错误信息：** 原sorter失败信息与旧活动结果混在一起。
**影响范围：** sorter比较和结果可信度。
**根本原因：** “当前选择”和“当前活动结果”缺少明确状态。
**修复方法：** 每个sorter独立记录pending/running/completed/failed；只有成功结果可激活；诊断页显示选中sorter的输入或结果，不代用其他结果。
**为什么这样修：** 不同sorter结果可以统一比较，但来源必须清晰。
**回归测试：** UI测试选择SpyKING CIRCUS 2时图中不出现Kilosort结果。
**修复结果：** **已回归**。
**剩余风险：** 每个外部sorter仍需独立真实安装和数据验证。

## BUG-012 SpikeInterface缓存文件句柄未关闭

**问题现象：** 30分钟sorting审计捕获 `unclosed file` ResourceWarning。
**出现位置：** SpikeInterface单线程二进制写入。
**如何复现：** linked Open Ephys记录首次建立sorting缓存。
**错误信息：** `ResourceWarning: unclosed file ...sorting_input_selected_channels.bin`。
**影响范围：** 批量动物处理、Windows文件占用。
**根本原因：** 当前SpikeInterface版本的 `n_jobs=1` worker context没有显式关闭内部文件字典。
**修复方法：** NeuroEphys AI适配层使用受控文件上下文，按内存预算动态分块读取并写入交错缓存；写完检查字节数。
**为什么这样修：** 适配层需要稳定的文件生命周期，同时保持源记录只读。
**回归测试：** Open Ephys测试逐值比较多分块缓存与源数据，捕获ResourceWarning为0；Subject105的30分钟真实数据运行未再出现未关闭文件警告。
**修复结果：** **已回归**。
**剩余风险：** 第三方sorter自身打开的临时文件仍由各工具负责关闭，NeuroEphys AI保留其原生日志。

## BUG-013 长记录Unit QC ACG耗时过高

**问题现象：** Subject104的13个unit Unit QC耗时409.46秒。
**出现位置：** 0–50 ms ACG。
**如何复现：** 30分钟、高spike数候选unit。
**错误信息：** 无异常，CPU时间过长。
**影响范围：** 批量动物验证和交互体验。
**根本原因：** 旧实现对50个bin分别执行整列二分搜索。
**修复方法：** 按相邻spike偏移量累计正向时间差，达到50 ms后停止；保留1 ms分箱。
**为什么这样修：** 减少重复二分搜索，同时维持原统计定义。
**回归测试：** `test_linear_acg_matches_original_bin_definition`逐箱等于旧公式；Subject105同一30分钟结果的Unit QC从394.403秒降至1.697秒，约提速232倍。
**修复结果：** **已回归**。
**剩余风险：** 极端高放电率、数千unit的高密度记录仍需单独做压力测试。

## BUG-014 GUI运行缺少结构化审计

**问题现象：** 批处理有JSONL日志，普通界面运行只有右侧文本记录。
**出现位置：** `PipelineWorker`。
**如何复现：** 从GUI运行任意节点后检查项目 `logs`。
**错误信息：** 无异常，缺少结构化文件。
**影响范围：** 可追溯性、耗时统计、失败恢复。
**根本原因：** 新审计上下文只接入验证脚本。
**修复方法：** GUI每个节点统一经过 `audited_stage`，保存输入、通道、片段、工具、参数、状态、耗时、输出、警告和恢复建议。
**为什么这样修：** GUI和自动化应产生同一审计结构。
**回归测试：** `test_gui_worker_writes_reloadable_structured_audit`检查running/completed记录并重开项目。
**修复结果：** **已回归**。
**剩余风险：** 外部工具直接写控制台的信息仍主要保存在其原生日志。

## BUG-015 外部sorter无法接收自定义Open Ephys读取器

**问题现象：** MountainSort5通过SpikeInterface子进程运行时无法反序列化NeuroEphys AI内部的Legacy读取对象。
**出现位置：** Open Ephys Legacy直接导入后运行外部sorter。
**如何复现：** 授权项目A选择MountainSort5并直接传递链接式记录对象。
**错误信息：** SpikeInterface子进程无法重建自定义`LegacyRecording`。
**影响范围：** 需要独立进程的外部sorter。
**根本原因：** 局部定义的读取类没有稳定的跨进程序列化入口。
**修复方法：** 先由NeuroEphys AI生成经过字节数校验的标准交错二进制缓存，再用SpikeInterface原生`read_binary`建立可序列化记录对象。源Open Ephys文件保持只读。
**为什么这样修：** 标准二进制对象可以被各sorter子进程一致恢复，也能复用同一缓存做公平比较。
**回归测试：** 授权项目A前30分钟MountainSort5、SpyKING CIRCUS2和Tridesclous2均从同一缓存完成运行。
**修复结果：** **已用真实数据验证**。
**剩余风险：** 其他记录系统需要继续验证其数据类型、通道顺序和缩放因子。

## BUG-016 Windows整段共享内存导致sorter失败

**问题现象：** SpyKING CIRCUS2尝试缓存30分钟记录时因系统资源不足退出；Tridesclous2采用相同路径时也会占用过多内存。
**出现位置：** SpikeInterface内部sorter的预处理缓存阶段。
**如何复现：** Windows、32通道、30分钟记录，使用默认整段共享内存缓存。
**错误信息：** `OSError: [WinError 1450] 系统资源不足`。
**影响范围：** 长记录、Windows和内存有限的工作站。
**根本原因：** 默认策略试图把整段记录放入共享内存。
**修复方法：** Windows下SpyKING CIRCUS2默认采用`no-cache`分块模式；Tridesclous2按块读取。采集元数据已注明在线AP滤波时，两个sorter均跳过重复带通预处理。
**为什么这样修：** 分块模式把内存占用限制在稳定范围，并避免对已经滤波的数据再次处理。
**回归测试：** 授权项目A前30分钟SpyKING CIRCUS2在202.249秒完成，Tridesclous2在306.172秒完成。
**修复结果：** **已用真实数据验证**。
**剩余风险：** 其他操作系统和超高密度记录仍需按硬件预算调整缓存模式。

## BUG-017 重跑sorter后沿用旧Unit QC

**问题现象：** MountainSort5重跑后得到89个候选cluster，摘要仍显示旧运行的96个QC条目。
**出现位置：** 多sorter结果注册、激活和比较摘要。
**如何复现：** 同一sorter键先完成一次QC，再用新参数强制重跑。
**错误信息：** 无异常；结果表中的unit数量与QC数量不一致。
**影响范围：** sorter比较、候选unit筛选、图表和审计可信度。
**根本原因：** 替换sorting结果时没有废弃该sorter键对应的派生QC缓存。
**修复方法：** 同名结果被替换时清除对应指标和诊断；若该结果处于活动状态，同时清除依赖它的事件分析、统计和机器学习结果。恢复项目时再次核对缓存unit编号与当前结果。
**为什么这样修：** Unit QC依赖具体的spike分配，任何重跑都需要重新计算。
**回归测试：** 新增`test_replacing_sorter_result_invalidates_stale_unit_qc`；修复后MountainSort5摘要为89个候选、89个QC条目。
**修复结果：** **已回归并经真实结果检查**。
**剩余风险：** 后续新增的派生模块也必须声明其sorting依赖。

## BUG-018 全时长Open Ephys缓存建立过慢

**问题现象：** 固定按1秒读取授权项目A完整记录，约4分钟仅完成6.4%，预计缓存耗时接近1小时。
**出现位置：** Open Ephys Legacy到sorter交错二进制缓存。
**如何复现：** 7,497秒、32通道记录建立首次缓存。
**错误信息：** 无异常；大量小块、逐通道读取造成性能瓶颈。
**影响范围：** 长时记录首次sorting体验。
**根本原因：** 固定1秒分块产生约7,500次循环，并在每次循环中访问32个通道文件。
**修复方法：** 按约64 MiB目标块大小动态计算帧数；32通道时每块约35秒，高密度记录会自动缩短时间块；连续数组直接写入文件。
**为什么这样修：** 内存预算稳定，文件访问次数显著减少，通道数变化时无需写死时长。
**回归测试：** 8项Open Ephys适配器测试通过；授权项目A全时长缓存约1至2分钟完成215个块，字节数校验通过。
**修复结果：** **已回归并经真实数据验证**。
**剩余风险：** 网络盘和机械硬盘速度受外部I/O限制。

## BUG-019 MED-PC事件流被保存为trial表

**问题现象：** 授权项目A的4,654个MED-PC事件在项目摘要中同时显示为4,654个trial。
**出现位置：** MED-PC C/D导入、同步和可复现导出。
**如何复现：** 导入C/D数组后查看 `state.events`、`state.trials` 和导出的 `trials.csv`。
**错误信息：** 无异常；语义错误。
**影响范围：** 行为图、trial计数、机器学习样本说明、AI项目摘要和复现报告。
**根本原因：** 旧实现为兼容通用事件分析，把每行event复制到 `state.trials`，并把事件顺序字段命名为 `trial`。
**修复方法：** 使用 `event_order` 保存事件顺序；MED-PC导入时保持 `state.trials=[]`；项目元数据写入 `trial_definition.status=not_defined`；加载旧项目时迁移事件字段并清空镜像trial表。
**为什么这样修：** 事件流和trial表承担不同语义。trial需要任务特定的起点、终点、结果和排除规则。
**回归测试：** `test_medpc_parser_and_ttl_sync`、`test_legacy_medpc_event_rows_are_not_restored_as_trials` 和同步回归测试通过；授权项目A全时长重新加载后为4,654个events、0个trials。
**修复结果：** **已回归并经真实数据验证**。
**剩余风险：** 正式trial规则仍需数据提供者补充实验范式定义。

## BUG-020 旧trials.csv在重新导出后残留

**问题现象：** 项目状态已修正为0个trial，但旧导出目录仍留有上一版本生成的 `trials.csv`。
**出现位置：** 在相同输出目录重复执行可复现导出。
**如何复现：** 先在有trial行的状态导出，再清空trial并导出到同一目录。
**错误信息：** 无异常；过期派生文件仍可见。
**影响范围：** 输出索引、用户复核、AI读取和下游脚本。
**根本原因：** 导出器只在有数据时写文件，没有在派生对象失效后删除对应旧文件。
**修复方法：** 当events或trials为空时删除同目录的过期CSV；当前项目保留 `events.csv` 并移除 `trials.csv`。
**为什么这样修：** 输出目录必须反映当前项目状态，不能混入旧语义文件。
**回归测试：** `test_export_removes_stale_trial_table_when_trials_are_not_defined`通过；授权项目A全时长导出重新生成后只保留 `events.csv`。
**修复结果：** **已回归并经真实数据验证**。
**剩余风险：** 后续新增派生文件需要声明失效和清理规则。

## BUG-021 AI候选工作流面板打开时报Qt未定义

**问题现象：** AI返回候选工作流后，渲染可勾选节点时发生 `NameError`，面板无法显示计划。
**出现位置：** AI assistant的Reviewable workflow plan。
**如何复现：** 给AI面板设置一个合法的QC候选节点并调用计划渲染。
**错误信息：** `NameError: name 'Qt' is not defined`。
**影响范围：** 助手模式和协作模式的可编辑工作流。
**根本原因：** 界面文件使用了 `Qt.ItemIsEnabled`、`Qt.ItemIsUserCheckable` 和 `Qt.Checked`，但漏导入Qt枚举。
**修复方法：** 从 `PySide6.QtCore` 显式导入Qt；在GUI测试中实际渲染一行候选计划并检查勾选状态。
**为什么这样修：** 该错误只在模型返回plan后触发，普通聊天路径无法覆盖。
**回归测试：** `test_ai_assistant_is_discoverable_and_plan_never_auto_runs`通过；英文AI产品截图成功生成。
**修复结果：** **已回归并经界面验证**。
**剩余风险：** 外部Provider返回的内容仍需JSON Schema和本地工作流规则双重校验。

## BUG-022 验证项目恢复后没有定位到待复核阶段

**问题现象：** 真实项目可以恢复候选Unit、事件和分析结果，但工作流状态为空，重新打开后无法自动定位到下一项人工任务；30分钟项目同时缺少 `logs/artifact_manifest.json`。
**出现位置：** 早期真实数据验证脚本生成的项目。
**如何复现：** 加载保存后的30分钟或全时长项目，检查 `workflow_status`、`metadata.last_open_step` 和产物清单。
**错误信息：** 无异常；恢复信息不完整。
**影响范围：** 项目续做、人工Unit复核、产物追溯和AI上下文。
**根本原因：** 验证脚本直接调用确定性分析函数，没有同步写入GUI使用的工作流状态；早期30分钟导出发生在产物清单模块接入之前。
**修复方法：** 在现有项目中补齐已完成节点，将 `unit_qc` 保持为待处理，并把 `last_open_step` 设置为 `unit_qc`；初始化活动sorter的人工复核容器；重建产物清单后再次保存和加载。
**为什么这样修：** 自动计算已经完成，专家复核仍未完成，恢复位置应停在需要研究人员判断的阶段。
**回归测试：** 全时长项目重新打开后恢复Kilosort4、12个候选Unit、2,195,626个spike、4,654个event、0个trial、LFP阻断和产物清单，界面当前页为 `unit_qc`；30分钟项目恢复五套sorter、AI历史和候选工作流。
**修复结果：** **已用真实保存项目验证**。
**剩余风险：** 新增工作流节点时需要给旧项目提供状态迁移规则。

## BUG-023 截取片段错误使用NEX5结束时间自动对齐

**问题现象：** 30分钟项目接入覆盖整段记录的NEX5结果时，若直接把两个结束时间相减，会得到看似合理、实际错误的时钟偏移。
**出现位置：** NEX5离线sorting导入与已有项目对齐。
**如何复现：** 建立带`start_frame/end_frame`的短片段项目，选择“自动结束对齐”。
**错误信息：** 旧逻辑可能无异常，属于高风险语义错误。
**影响范围：** sorter匹配、Unit QC、事件分析和对比结论。
**根本原因：** 片段结束时间不代表原始记录结束时间，缺少唯一的自动对齐约束。
**修复方法：** 检测项目的recording adapter是否声明片段范围；片段项目直接拒绝自动推断，要求保留同一时钟或填写同步证据支持的人工偏移。
**为什么这样修：** 错误时间偏移会让真实匹配消失，也可能制造偶然匹配，程序应当在证据不足时停止。
**回归测试：** `test_nex5_auto_alignment_rejects_recording_segments`构造片段项目并确认抛出明确错误；真实30分钟项目使用155.916533秒人工偏移后完成比较。
**修复结果：** **已回归并经真实结果检查**。
**剩余风险：** 人工偏移仍需用户用采集时间、同步脉冲或共同Unit证据复核。

## BUG-024 导入只读sorting后可能误运行或显示旧Kilosort结果

**问题现象：** 外部NEX5结果接入项目后，如果仍沿用可执行sorter的按钮语义，用户可能误以为导入结果可以再次运行；旧Kilosort图也可能继续显示。
**出现位置：** Spike sorting工作台、sorter选择和运行当前节点。
**如何复现：** 项目已有Kilosort4，导入一个NEX5结果，切换到新结果后点击运行。
**错误信息：** 无异常；界面状态和结果来源表达不清。
**影响范围：** 结果归属、审计可信度和用户对黑箱运行的判断。
**根本原因：** sorter目录只列出本机可执行后端，没有给项目内只读结果单独状态。
**修复方法：** 把项目中缺少执行后端的结果添加为虚拟只读行；显示来源、Unit数和spike数；运行时提示进入Unit QC或改选可执行sorter；图表始终按当前活动结果刷新。
**为什么这样修：** 导入结果与重新sorting承担不同操作，界面需要明确区分。
**回归测试：** `test_sorter_workbench_lists_imported_read_only_results`检查只读行可选择；NEX5导入测试检查保存、重开和来源字段；真实项目切换后显示外部8个候选Unit。
**修复结果：** **已回归并经真实项目验证**。
**剩余风险：** 新增外部格式时必须注册可读的来源名称和专属诊断能力。

## BUG-025 Unit QC缺少跨Unit重复时间戳筛查

**问题现象：** 单个Unit的ISI和波形可通过初筛，但两个候选Unit可能共享大量近同时spike，用户难以及时发现重复检测、串扰或拆分风险。
**出现位置：** Unit QC与人工复核。
**如何复现：** 导入同一记录的外部NEX5候选Unit，逐个查看单Unit指标。
**错误信息：** 无异常；缺少跨Unit证据。
**影响范围：** single-unit、multi-unit和重复cluster的人工分类。
**根本原因：** 原指标只在每个Unit内部计算，没有一对一统计不同Unit在0.1毫秒窗口内的时间戳重合。
**修复方法：** 新增线性内存的一对一时间戳重合筛查；保存匹配spike数、双向重合比例、主通道和可能重复标志；界面显示最高重合和对应Unit。程序不自动删除或合并任何候选。
**为什么这样修：** 重合证据可安排人工复核优先级，同时保留研究人员对生理同步、串扰和sorting错误的判断权。
**回归测试：** `test_duplicate_screen_flags_overlap_without_dropping_units`确认高重合被标记且Unit数量不变；真实NEX5结果发现三对超过20%的重合，其中一对超过80%。
**修复结果：** **已回归并经真实结果检查**。
**剩余风险：** 高同步放电、公共伪迹和重复检测均可产生重合，必须结合波形、通道、ACG和原sorter界面判断。

## BUG-026 产品截图脚本无法从仓库根目录启动

**问题现象：** 执行产品截图脚本时立即退出，无法生成报名和文档使用的英文界面截图。
**出现位置：** `scripts/capture_product_screenshots.py` 启动阶段。
**如何复现：** 在仓库根目录运行 `python scripts/capture_product_screenshots.py`。
**错误信息：** `ModuleNotFoundError: No module named 'neuroflow'`。
**影响范围：** 产品截图更新、界面回归检查和公开文档素材。
**根本原因：** Python以脚本所在的 `scripts` 目录作为首个模块搜索路径，仓库根目录没有进入 `sys.path`。
**修复方法：** 脚本在导入NeuroEphys AI模块前解析仓库根目录，并仅在缺失时加入模块搜索路径；后续输出路径复用同一解析结果。
**为什么这样修：** 脚本可从任意当前目录启动，同时不会依赖用户手动设置 `PYTHONPATH` 或安装开发版包。
**回归测试：** 从仓库根目录重新运行脚本，确认首页、项目导入、sorting、同步、Unit人工复核、AI助手和Figure Studio英文截图均生成成功。
**修复结果：** **已回归**。
**剩余风险：** 无显示桌面的CI环境不能直接截取原生窗口，自动流水线仍应使用专门的虚拟显示环境。

## BUG-027 干净安装环境缺少sorter比较依赖

**问题现象：** GitHub发布流水线安装公开核心依赖后，四项sorting恢复、切换和比较测试失败；开发机测试正常。
**出现位置：** SpikeInterface多sorter结果比较与相关界面测试。
**如何复现：** 在不包含开发机传递依赖的干净Python环境中安装 `requirements-core.txt`，随后运行完整测试。
**错误信息：** `ModuleNotFoundError: No module named 'networkx'`。
**影响范围：** Kilosort、MountainSort及其他标准化sorting结果之间的匹配和比较；公开Windows预览包的自动构建。
**根本原因：** SpikeInterface的 `MultiSortingComparison` 运行时需要NetworkX，但公开核心依赖清单没有把它声明为NeuroEphys AI的直接运行依赖。开发环境曾由其他包间接安装NetworkX，掩盖了问题。
**修复方法：** 在核心和完整依赖清单中显式固定 `networkx==3.6.1`，使本地开发、干净安装和发布构建使用同一兼容版本。
**为什么这样修：** NeuroEphys AI直接使用多sorter比较能力，因此其运行依赖应由本项目明确管理，不能依赖第三方包当前是否偶然传递安装。
**回归测试：** 最初失败的四项测试单独重跑为4/4通过；完整测试为74/74通过；中英文Sphinx文档严格构建通过。
**修复结果：** **已回归，等待公开发布流水线复核**。
**剩余风险：** 第三方库以后可能调整可选依赖；发布流水线继续保留干净环境全量测试用于提前发现变化。
