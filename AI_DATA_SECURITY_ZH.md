# NeuroEphys AI 数据安全说明

## 1. 数据处理边界

原始电生理、行为文件、视频和项目派生数据默认保留在本机。在线 AI 接收选定的结构化摘要、用户问题和脱敏日志。v1.2.5 的 Harness 查询功能在明确允许后，还可发送分页事件、trial、spike 时间、Unit 指标、分析结果及保存的项目对话。它不是“仅发送计数”，也不承诺所有科学字段已经完全匿名化；请按机构政策选择是否开放查询。

Harness SDK 模式不读取或复制机构模型密钥，由本机官方 dsh 负责身份认证。App 的 MCP 服务仅监听 127.0.0.1，使用每轮随机认证令牌；不对局域网开放，不提供任意文件读取或代码执行。

## 2. 请求前预览

每次在线请求前，用户可以打开“Preview cloud data”检查字段。软件按类别展示：

- 项目结构；
- 记录摘要；
- 采集设置；
- QC；
- sorting 与 Unit；
- 行为与同步；
- 统计与机器学习；
- 工作流状态；
- 当前页面上下文；
- 可选日志。

用户可以取消请求或移除可选字段。

当前打开项目首次发送会要求确认类别，后续继续沿用；切换项目或服务设置后重新确认，也可随时重新预览。取消“按需查询项目数据、结果与历史对话”会关闭 MCP 实际数据查询。请求使用发送时快照，不是永久监视。提问内容和近期对话另行发送，勿在自由文本中粘贴密钥。

## 3. 路径和身份信息

发送前执行：

- 删除 `recording_path`、`behavior_path` 和 `project_root`；
- 删除 Windows 盘符、UNC 路径和用户目录；
- 删除未明确允许的 subject、动物和人员字段；
- 把错误日志中的本机路径替换为占位符；
- 仅保留分析所需的计数、单位、参数和状态。

项目内可以保留完整路径用于恢复。公开导出和在线请求使用脱敏摘要。

## 4. API 密钥

API 密钥：

- 可只保留在运行内存；
- 可存入 Windows Credential Manager 等操作系统凭据区；
- 可通过环境变量提供；
- 不写入 `neuroflow_project.json`；
- 不写入 `structured_runs.jsonl`；
- 不写入 `provenance.json`；
- 不写入 AI 对话历史；
- 不写入 Git 仓库；
- 不显示在截图和报告。

“读取本机 DeepSeek Harness 配置”只打开 Harness 的非敏感 `settings.yaml`，不打开它的凭据文件。App 所需凭据由环境变量、当前会话或操作系统凭据库独立提供。

如机构只提供内网 HTTP 端点，App 默认拒绝。只有用户对该机构内网 Provider 显式启用后才可连接；这不会放宽公网 HTTP 或其他 Provider 的限制。

## 5. AI 请求审计

每次请求记录：

- 时间；
- Provider；
- 模型；
-发送字段类别；
- 用户授权；
- 任务类型；
- 结果用途；
- 状态和耗时。

审计不记录密钥、原始电压、完整本机路径和不必要的敏感文本。

## 6. 受控工具调用

模型只能提出白名单工具。NeuroEphys AI 在本地验证：

- 工具是否存在；
- 参数是否符合 JSON Schema；
- 输入是否完整；
- 前置节点是否完成；
- 数据类型是否允许；
- 是否会覆盖结果；
- 是否属于高成本操作；
- 是否需要确认。

原始数据删除、源文件覆盖和未经授权的对外传输被禁止。

## 7. 科研数据承诺对应关系

1. 参赛材料使用模拟数据、公开数据或经团队授权并完成必要脱敏的内部验证摘要。
2. 第三方 AI 工具的发送范围在请求预览和审计中记录。
3. 公开数据来源、许可证、论文和官方文档记录在 `THIRD_PARTY_SOURCES.md`。
4. 开源算法通过适配器调用，原生输出和版本保留；NeuroEphys AI 的工作流、界面、适配、规则、审计和教程为项目代码。
5. 项目权利说明见 `PROJECT_RIGHTS_NOTICE_ZH.md`。

## 8. 当前验证状态

已验证：

- 路径从结构化请求中移除；
- API 密钥不进入请求和项目日志；
- 原始电压不进入请求；
- AI关闭时全部手动功能可用；
- Provider断开时软件返回错误并保持项目可用；
- 授权真实项目摘要阻断虚假LFP建议；
- 项目保存和重开后恢复对话与已批准工作流。

已额外完成：

- 实机 DeepSeek Harness 非敏感配置发现；
- 机构内网模型列表、最小聊天请求和受控工具建议验证；
- 非白名单工具不会进入待执行列表；
- 私有 HTTP 只在对单个 Provider 显式开启后允许。

待验证：

- Ollama本地服务；
- macOS Keychain和Linux Secret Service的实机凭据保存。
